# Song-Fetcher-and-with-Stats
Fetches song data from iTunes API based on artist (via command-line arg), saves to CSV, then reads back to compute basic stats (count tracks, random pick one) and validate track names with regex. Shows API handling, JSON parsing, CSV write/read, random, loops, conditionals, regex
